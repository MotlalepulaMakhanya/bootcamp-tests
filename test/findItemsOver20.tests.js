describe('findItemsOver20 Function test' , function(){
    it('Function that takes a list of objects, each object having 2 attributes one called name and the other qty. The function should return all the products that have a quantity higher than 20' , function(){
        assert.equal(2,2);

        assert.deepEqual([2,2],[2,2]);
    });

});
